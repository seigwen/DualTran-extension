node_modules
package-lock.json
extra/package-lock.json
dist
test
meta.txt
result
*.zip" " 
/screenshots
/.codegraph
/.mcp.json
# Agent scratch files (throwaway scripts / logs) — never commit these.
# Convention: prefix ad-hoc debugging scripts with .tmp- (see CLAUDE.md).
.tmp-*
.tmp-*.mjs
.tmp-*.sh
